#include "main.h"
#include <string.h>

/**
 * _strcat - concatenating two strings
 * @dest: the destination of the two strings
 * @src: the source string
 *
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}