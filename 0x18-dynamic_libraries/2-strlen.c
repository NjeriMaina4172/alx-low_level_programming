#include "main.h"
#include <string.h>

/**
 * _strlen - find the string lenght
 * @s: first parameter
 * Return: the length of s
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}