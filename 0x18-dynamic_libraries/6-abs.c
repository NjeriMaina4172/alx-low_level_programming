#include <stdlib.h>
#include "main.h"

/**
 * _abs - returning the absolute value
 * @n: the main parameter
 *
 * Description: returning the absolute value of a number
 * Return: Always 0 (success)
 */

int _abs(int n)
{
	return (abs(n));
}