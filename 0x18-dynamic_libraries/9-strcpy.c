#include "main.h"
#include <string.h>

/**
 * _strcpy - copy the contents of an array
 * @dest: address to copy to
 * @src: address to copy from
 *
 * Return: pointer to destination
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}